const run = require('./run');
const configure = require('./configure');
const customise = require('./customise');
const when = require('./when');

module.exports = { run, configure, customise, when };
